import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'VDG - Política de Privacidade',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
